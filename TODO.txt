#TODO list

Query 6 hours instead of 6 1hour segments?

Create function to parse friends, (initially, just have comma if multiple friends, and parse that to know what friends they have) DONE

Create a function to retrieve data from the mongoDB, based on friends input DONE

convert the JSON to an easy to read format, viewable to the user

Add location data to JSON data stored in database

On display show approximate location along with availibility

Integrate front end with backend

DEPLOYMENT